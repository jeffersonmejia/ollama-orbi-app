@if (TempData["Success"] is string success)
{
    <div class="orbi-alert" role="status">@success</div>
}
@if (TempData["Error"] is string error)
{
    <div class="orbi-alert orbi-alert-error" role="alert">@error</div>
}
